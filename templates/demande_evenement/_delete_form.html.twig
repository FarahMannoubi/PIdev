<form method="post" action="{{ path('demande_evenement_delete', {'id': demande_evenement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_evenement.id) }}">
    <button class="btn">Delete</button>
</form>
